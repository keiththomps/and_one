#!/usr/bin/env bash
set -euo pipefail

bundle install

# Install git pre-commit hook for linting
echo "Installing git hooks..."
git config core.hooksPath .githooks
echo "Done! Pre-commit hook will run RuboCop on staged files."
